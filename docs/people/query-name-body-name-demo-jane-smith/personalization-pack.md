# Personalization Pack — {{.query?.name || .body?.name || 'Demo Jane Smith'}}

## Brand strategy
AI-ready professional profile for {{.query?.name || .body?.name || 'Demo Jane Smith'}}

## LinkedIn bio rewrite
{{.query?.name || .body?.name || 'Demo Jane Smith'}} — Professional profile concept for {{.query?.name || .body?.name || 'Demo Jane Smith'}}. Focused on clear communication, useful public context, and opportunity-ready positioning.

## Intro email
Subject: Quick profile-site concept for {{.query?.name || .body?.name || 'Demo Jane Smith'}}

Hi {{.query?.name, I made a quick public-source profile-site concept showing how your professional signals can become a polished personal brand page. It includes source receipts, confidence scoring, and multiple site variants.

## Speaker intro
{{.query?.name || .body?.name || 'Demo Jane Smith'}} is presented here through a consent-based AI profile demo that turns public/source-backed signals into a concise digital presence.

## Tagline options
- "Meet {{.query?.name || .body?.name || 'Demo Jane Smith'}}, through a sharper digital profile."
- "Public signals turned into a polished personal brand page."
- "A profile site built from evidence, not guesswork."
